# Static security headers (Netlify / Cloudflare Pages compatible)
# If you use GitHub Pages directly, these may not apply; see SECURITY_HEADERS.md

/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=(), usb=()
  X-Frame-Options: DENY
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Resource-Policy: same-origin
  Cross-Origin-Embedder-Policy: unsafe-none

  # HSTS: only enable if the site is ALWAYS served over HTTPS
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

  # CSP tuned for: Font Awesome CDN + Google Analytics (gtag)
  # If you add new CDNs/scripts, update this.
  Content-Security-Policy: default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'; form-action 'self'; upgrade-insecure-requests; script-src 'self' https://www.googletagmanager.com https://www.google-analytics.com 'unsafe-inline'; connect-src 'self' https://www.google-analytics.com https://region1.google-analytics.com https://www.googletagmanager.com; img-src 'self' data: https:; style-src 'self' https://cdnjs.cloudflare.com 'unsafe-inline'; font-src 'self' https://cdnjs.cloudflare.com https://fonts.gstatic.com data:; manifest-src 'self'
